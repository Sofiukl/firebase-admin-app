{
  "name": "firebase-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm install && npm run start"
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
